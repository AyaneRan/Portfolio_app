
<li>
  <%= link_to user.name, user %>

  <% if current_user&.admin? && current_user != user %>
    |
    <%= link_to "delete", user,
          data: { "turbo-method": :delete, "turbo-confirm": "Are you sure to delete this?" } %>
  <% end %>
</li>