<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Portfolio App" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- CSS は application を読むのが正解 -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%= yield :head %>
  </head>
  <body>
    <!-- フラッシュ：end を忘れない！ -->
    <% flash.each do |key, message| %>
      <div class="alert alert-<%= %w[success notice].include?(key.to_s) ? "success" : "danger" %>">
        <%= message %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html
